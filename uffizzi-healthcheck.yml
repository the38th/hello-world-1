services:
  nginx:
    image: zipofar/my_nginx_simple:latest
    healthcheck:
      test:
        - CMD
      interval: 10s
      timeout: 5s
      start_period: 5
      retries: 5

x-uffizzi:
  ingress:
    service: nginx
    port: 80
